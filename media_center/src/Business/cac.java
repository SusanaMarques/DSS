package Business;

public class cac {


    /*public Map<Integer,String> getPlaylistMusica(int idPlaylist,int idU) {


        Playlist p = playlistsMusicas.get(idPlaylist);
        Map<Integer,String> mp = new HashMap<>();
        mp.put(idPlaylist,p.getNome());
        for(int idm : (p.getlst())){
            if(categoriasMusica.containsKey(idm)) mp.put(idm,categoriasMusica.get(idm).get(idU));
        }

        return mp;
    }

    public Playlist getPlaylistMusicas (int idUtilizadorAtual) {
        Set<Integer> catmus =categoriasMusica.keySet();
        Map<Integer,String>  m_mudada = null;
        for(Map.Entry<Integer,String> e: m_mudada.entrySet()){
            for(Integer mm: catmus){
                m_mudada.put(mm,e.getValue());
            }
        }

    }
}*/
}
